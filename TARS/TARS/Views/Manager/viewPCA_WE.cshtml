@model TARS.Models.PCA_WE

@{
    ViewBag.Title = "viewPCA_WE";
}

<h2>Details</h2>

<fieldset>
    <legend>PcaCode</legend>

    <div class="display-label">PCA Code</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.PCA)
    </div>

    <div class="display-label">Work Effort</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.WE)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "editPCA_WE/", new { id=Model.ID }) |
    @Html.ActionLink("Back to List", "searchPCA_WE/")
</p>
